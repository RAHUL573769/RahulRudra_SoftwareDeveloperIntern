import React from "react";
import { Link } from "react-router-dom";
import "./Header.css";

const Header = () => {
  return (
    <div>
      <nav>
        <a href="/">Users</a>
      </nav>
    </div>
  );
};

export default Header;
